
<%= simple_form_for([job, job_request]) do |f| %>
  <%= f.input :current_status, :collection => ['pending', 'accepted', 'refused'], input_html: { class: 'text-small-size dropdown-list', value: job_request.current_status }, label: false%>
  <%= f.submit "add a review", class: "btn btn-primary delete-btn" %>
<% end %>


()

